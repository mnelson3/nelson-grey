# Example environment variables for runner-manager
# Copy this to .env and fill with real values (do NOT commit .env)

# Comma-separated API keys for runners (give at least one)
RUNNER_MANAGER_API_KEYS=your_api_key_here

# Webhook secret configured in the GitHub App settings
RUNNER_MANAGER_WEBHOOK_SECRET=your_webhook_secret_here

# GitHub App numeric ID
GITHUB_APP_ID=123456

# Path to the private key file on disk (do NOT commit the key)
GITHUB_APP_PRIVATE_KEY_PATH=/path/to/app-private-key.pem

# Port to run the manager on
PORT=3001
