# OS
.DS_Store

# Node
node_modules/

# Runner artifacts (never commit)
actions-runner/
runner-manager/.env
**/_work/
**/_diag/

# Local deploy keys / private keys (never commit)
match_deploy_key
match_deploy_key.pub
*.pem

# Local env files (never commit)
.env
.env.*

# Secrets and Certificates
certs/
profiles/

